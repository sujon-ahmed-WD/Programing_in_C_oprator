#include <stdio.h>
#include <stdbool.h>
int main()
{
    int tourDayse = 4;
    if (tourDayse == 2)
    {
        bool isBootavale = false;
        if (isBootavale)
        {
            printf("go to nangorHour\n");
        }
        else
        {
            printf(" go to Shimangle\n");
        }
    }
    else if (tourDayse == 4)
    {
        bool isBootavale = false;
        if (isBootavale)
        {
            printf("go to santmartin\n");
        }
        else

        {
            printf("go to coxbajer\n");
        }
    }
}